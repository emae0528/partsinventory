<%= simple_form_for(item) do |f| %>

	<%= f.input :name %>
	<%= f.input :quantity %>
	<%= f.input :price %>
	<%= f.input :source %>
	<%= f.input :status %>
	<%= f.association :member %>
	<%= f.button :submit %>

<% end %>